// ============================================================================
// 🤖 وحدة الأنواع الأساسية للوكيل - Agent Types Module
// ============================================================================
// تعريف جميع الأنواع والهياكل المستخدمة في الوكيل المساعد

// ============================================================================
// 1. نوع النية (Intent Type)
// ============================================================================
struct Intent {
    intent: string,      // نوع النية: "run", "compile", "analyze", "suggest", "chat"
    target: string,      // المسار أو الملف المستهدف
    code: string,        // الكود المراد تحليله
    hint: string,        // تلميح أو معلومة إضافية
}

// ============================================================================
// 2. نوع الاستجابة (Response Type)
// ============================================================================
struct Response {
    intent: string,      // نوع النية الأصلية
    text: string,        // النص المراد طباعته
    data: list<string>,  // البيانات الإضافية
    ok: bool,            // هل العملية نجحت؟
}

// ============================================================================
// 3. نوع الإعدادات (Config Type)
// ============================================================================
struct Config {
    language: string,         // اللغة: "ar" أو "en"
    dry_run: bool,            // هل نعمل في وضع المحاكاة؟
    bayan_binary: string,     // مسار البرنامج التنفيذي
    runtime_cwd: string,      // مجلد العمل
}

// ============================================================================
// 4. نوع الحدث (Event Type)
// ============================================================================
struct Event {
    intent: string,      // نوع النية
    ok: bool,            // هل نجحت العملية؟
    timestamp: int,      // الوقت
}

// ============================================================================
// 5. نوع الوكيل (Agent Type)
// ============================================================================
struct BayanAgent {
    config: Config,      // الإعدادات
    events: list<Event>, // سجل الأحداث
}

// ============================================================================
// دوال مساعدة لإنشاء الأنواع
// ============================================================================

// إنشاء نية جديدة
fn create_intent(intent: string, target: string, code: string, hint: string) -> Intent {
    return Intent {
        intent: intent,
        target: target,
        code: code,
        hint: hint,
    };
}

// إنشاء استجابة جديدة
fn create_response(intent: string, text: string, ok: bool) -> Response {
    return Response {
        intent: intent,
        text: text,
        data: [],
        ok: ok,
    };
}

// إنشاء إعدادات جديدة
fn create_config(language: string, dry_run: bool) -> Config {
    return Config {
        language: language,
        dry_run: dry_run,
        bayan_binary: "target/debug/albayan",
        runtime_cwd: ".",
    };
}

// إنشاء حدث جديد
fn create_event(intent: string, ok: bool) -> Event {
    return Event {
        intent: intent,
        ok: ok,
        timestamp: 0,
    };
}

// إنشاء وكيل جديد
fn create_agent(config: Config) -> BayanAgent {
    return BayanAgent {
        config: config,
        events: [],
    };
}

// ============================================================================
// دوال مساعدة للتحقق
// ============================================================================

// التحقق من أن النية صحيحة
fn is_valid_intent(intent: string) -> bool {
    if intent == "run" { return true; }
    if intent == "compile" { return true; }
    if intent == "analyze" { return true; }
    if intent == "suggest" { return true; }
    if intent == "chat" { return true; }
    return false;
}

// التحقق من أن اللغة صحيحة
fn is_valid_language(language: string) -> bool {
    if language == "ar" { return true; }
    if language == "en" { return true; }
    return false;
}

// ============================================================================
// نهاية وحدة الأنواع
// ============================================================================

