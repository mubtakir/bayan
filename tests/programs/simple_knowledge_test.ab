//! Simple Knowledge Engine Test
//! اختبار بسيط لمحرك المعرفة الدلالية

/// اختبار بسيط جداً
fn test_simple() {
    let x = 1.0;
    let y = 2.0;
    let z = x + y;
}

/// الدالة الرئيسية للاختبار
fn main() {
    test_simple();
}
