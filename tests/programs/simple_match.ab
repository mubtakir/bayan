// Simple match statement test for AlBayan language
// Testing basic pattern matching as recommended by expert

fn main() -> int {
    let x = 5;
    
    match x {
        1 => {
            return 10;
        }
        5 => {
            return 50;
        }
        _ => {
            return 0;
        }
    }
}
