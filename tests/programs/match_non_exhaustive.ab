// Test non-exhaustive match (should fail)
fn test_bool_non_exhaustive() -> int {
    let x = true;
    
    // This should fail - missing false case
    match x {
        true => 1
    }
}

fn main() -> int {
    return test_bool_non_exhaustive();
}
