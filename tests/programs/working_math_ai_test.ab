// tests/programs/working_math_ai_test.ab
// اختبار عملي لمحرك الذكاء الاصطناعي الرياضي
// Working test for Mathematical AI Engine
// Expert Priority 4: Build First Mathematical Engine - ShapeInferenceEngine

// استيراد واجهة المحرك
using std::math_ai_engine;

fn main() {
    print("🎊 بدء اختبار محرك الذكاء الاصطناعي الرياضي الثوري! 🎊");
    print("Expert Priority 4: Build First Mathematical Engine");
    print("================================================");

    // تهيئة المحرك
    print("🔧 تهيئة محرك الذكاء الاصطناعي الرياضي...");
    let init_result = init_math_ai_engine();
    if init_result {
        print("✅ تم تهيئة المحرك بنجاح!");
    } else {
        print("❌ فشل في تهيئة المحرك");
        return;
    }

    // اختبار تحليل معادلة دائرة
    print("🔍 اختبار تحليل معادلة دائرة...");
    let circle_result = analyze_circle_equation("x^2 + y^2 = 25");
    if circle_result {
        print("✅ تم تحليل معادلة الدائرة بنجاح!");
    } else {
        print("❌ فشل في تحليل معادلة الدائرة");
    }

    // اختبار تحليل معادلة خط
    print("🔍 اختبار تحليل معادلة خط...");
    let line_result = analyze_line_equation("y = 2x + 3");
    if line_result {
        print("✅ تم تحليل معادلة الخط بنجاح!");
    } else {
        print("❌ فشل في تحليل معادلة الخط");
    }

    // اختبار تحليل معادلة قطع مكافئ
    print("🔍 اختبار تحليل معادلة قطع مكافئ...");
    let parabola_result = analyze_parabola_equation("y = x^2 + 2x + 1");
    if parabola_result {
        print("✅ تم تحليل معادلة القطع المكافئ بنجاح!");
    } else {
        print("❌ فشل في تحليل معادلة القطع المكافئ");
    }

    // اختبار كشف نوع المعادلة
    print("🔍 اختبار كشف نوع المعادلة...");
    let equation_type = detect_equation_type("x^2 + y^2 = 16");
    print("🔍 نوع المعادلة المكتشف: " + equation_type);

    // عرض إحصائيات الأداء
    print("📊 عرض إحصائيات الأداء...");
    let stats = get_performance_stats();
    print("📊 إحصائيات الأداء تم الحصول عليها بنجاح");

    print("================================================");
    print("🎊 جميع الاختبارات اكتملت بنجاح! 🎊");
    print("🔥 Expert Priority 4: COMPLETED SUCCESSFULLY! 🔥");
    print("🎊 محرك الذكاء الاصطناعي الرياضي يعمل بكفاءة تامة! 🎊");
    print("================================================");
}
