// Test 7: Complex arithmetic expressions
// Expected output: 23 ((10 + 5) * 2 - 7)

fn main() -> int {
    let a: int = 10;
    let b: int = 5;
    let c: int = 2;
    let d: int = 7;
    return (a + b) * c - d;
}
